"use strict";
/* eslint-env mocha */
const expect = require("chai").expect;

describe("Octoglance", function () {
  beforeEach(() => {});

  it("should be tested", function () {
    expect(true).to.equal(true);
  });
});
